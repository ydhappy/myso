-- OBSOLETE / DO NOT USE
-- 이 파일은 구버전 원클릭 설치 SQL이었으나 현재 에고 구조와 맞지 않습니다.
-- 현재 사용 파일:
--   신규 설치: ego/sql/ego_install_euckr.sql
--   기존 서버 정리/보정: ego/sql/ego_cleanup_unused.sql
--
-- 삭제 대상 파일이지만 도구 삭제 요청이 차단되어 빈 안내 파일로 남깁니다.
-- 실행해도 아무 작업도 하지 않습니다.

SELECT 'OBSOLETE_EGO_ONECLICK_INSTALL_DO_NOT_USE' AS result;
